<project name="nuxeo-shibboleth-invitation-tests" xmlns:nx="urn:nuxeo-build" xmlns:artifact="urn:nuxeo-artifact">
  <taskdef resource="org/nuxeo/build/antlib.xml" uri="urn:nuxeo-build" />
  <taskdef resource="org/nuxeo/build/artifact/antlib.xml" uri="urn:nuxeo-artifact" />
  <taskdef resource="net/sf/antcontrib/antlib.xml" />

  <!-- Base distribution (nuxeo-cap, nuxeo-dm, ...) -->
  <property name="out.dir" value="${maven.project.build.directory}" />
  <unzip dest="${out.dir}/" overwrite="false">
    <artifact:resolveFile key="org.nuxeo:nuxeo-ftest::zip" />
  </unzip>
  <import file="${out.dir}/nuxeo-ftest.xml" />
  <property name="mp.install" value="file:${out.dir}/marketplace-shibboleth-invitation-${maven.project.version}.zip" />
  <!-- Override default prepare-environment target to make it download your MP -->
  <target name="prepare-environment" depends="_init,prepare-db,prepare-tomcat">
    <copy todir="${out.dir}">
      <artifact:file key="org.nuxeo.shibboleth.invitation.marketplace:marketplace-shibboleth-invitation::zip" />
    </copy>
  </target>

</project>
